const searchMenuInitialState = { query: '' };
export default function searchMenu(state = searchMenuInitialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}